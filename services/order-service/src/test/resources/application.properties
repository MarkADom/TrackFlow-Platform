# Disable OpenTelemetry SDK so @WebMvcTest doesn't try to connect to localhost:4318
otel.sdk.disabled=true

# Minimal Kafka bootstrap to satisfy property binding; no real connection is made in web-slice tests
spring.kafka.bootstrap-servers=localhost:9093
