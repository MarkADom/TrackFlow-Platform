meta {
  name: Get Failed Notifications
  type: http
  seq: 2
}

get {
  url: {{notification_service_url}}/api/v1/notifications/failed
  body: none
  auth: none
}

tests {
  test("Status is 200", function() {
    expect(res.status).to.equal(200);
  });
  test("Returns array", function() {
    expect(res.body).to.be.an("array");
  });
}
